export default class Builder {
    constructor(lesson) {
        // let keys = Object.keys(lesson.lesson)
        // switch (lesson.lesson.) {
        //     case value:

        //         break;

        //     default:
        //         break;
        // }
    }
}